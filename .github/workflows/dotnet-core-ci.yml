name: .NET Core

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1.5.0

    - name: Install dependencies
      run: dotnet restore source/Library/Library.csproj

    - name: Build
      run: dotnet build source/Library/Library.csproj --configuration Release --no-restore

    - name: Generate Nuget package
      run: dotnet pack source/Library/Library.csproj --configuration Release

    - uses: actions/upload-artifact@v2
      with:
        path: source/Library/bin/Release/Library.1.0.0.nupkg

    - name: NuGet push
      run: dotnet nuget push "source/Library/bin/Release/Library.1.0.0.nupkg" --source https://nuget.pkg.github.com/adamsimsy/github-actions-azure-devops-integration-examples/index.json --api-key ${{github.token}} --skip-duplicate --no-symbols true
      env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}